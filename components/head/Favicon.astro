---
import { config } from '../../config'
interface Props {
    favicon?: string;
}

const { 
    favicon 
} = Astro.props;
---

<link 
    rel="icon" 
    type="image/svg+xml" 
    href={favicon} 
/>
