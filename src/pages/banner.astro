---
import Layout from "../layouts/Layout.astro";
import Banner1 from "../components/section/banner/Banner1.astro";
import Banner2 from "../components/section/banner/Banner2.astro";

const bannerData = {
    title: "Impulsa tu negocio con nuestra solución",
    description:
        "Nuestro equipo ofrece herramientas <strong>personalizadas</strong> para tu crecimiento.",
    buttonText: "Ver más",
    buttonHref: "#servicios",
    buttonType: "button" as const,
};

const bannerSinBoton = {
    title: "Este es un banner sin botón",
    description: "Podés usarlo para mensajes importantes sin CTA.",
};
---

<Layout title="Banners" description="Componentes UI modernos y reutilizables para proyectos Astro + Tailwind">
    <Banner1
        sectionId="banner-prueba"
        sectionClass="py-24"
        containerId="banner-contenedor"
        containerClass="max-w-3xl mx-auto"
        title={bannerData.title}
        titleClass="text-3xl md:text-4xl font-semibold"
        description={bannerData.description}
        descriptionClass="text-base"
        buttonText={bannerData.buttonText}
        buttonHref={bannerData.buttonHref}
        buttonType={bannerData.buttonType}
    />

    <Banner1
        sectionId="banner-sin-boton"
        sectionClass="py-24 bg-pink-100"
        containerId="container-sin-boton"
        containerClass="text-center"
        title={bannerSinBoton.title}
        titleClass="text-2xl font-bold"
        description={bannerSinBoton.description}
        descriptionClass="text-gray-700"
    />

    <Banner2
        title="Transforma tu presencia online"
        description="Creamos soluciones digitales a medida. Contactanos hoy mismo y llevá tu proyecto al siguiente nivel."
        buttonText="Conocé más"
        buttonHref="/servicios"
    />
</Layout>
